apiVersion: v1
kind: Service
metadata:
  name: travel-card-api
  namespace: travelcard
  labels:
    app: travel-card-api
spec:
  type: ClusterIP
  selector:
    app: travel-card-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
