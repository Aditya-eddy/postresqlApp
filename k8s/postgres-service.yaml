apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: travelcard
spec:
  clusterIP: None
  selector:
    app: postgres
  ports:
    - name: pgsql
      port: 5432
      targetPort: 5432
